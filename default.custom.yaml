# personal global patch over default.yaml
# user layer file never overwritten by update-upstream.sh
patch:
  # enable only the mint flypy hybrid schema
  schema_list:
    - schema: rime_mint_flypy

  # candidate count global default
  menu/page_size: 8
